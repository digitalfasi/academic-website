# Academy imagery

Expected files
- hero-poster.jpg      1920x1080 poster frame for the hero video.
- Real project screenshots for the "What you build" section: add the path as
  `image` on the matching entry in DELIVERABLES (lib/academy/content.ts) to
  replace the schematic sample frame.
- Trainer portraits: add as `image` on the matching entry in TRAINERS.

The official DFX Academy logo goes to /public/logo/dfx-academy-logo.png.
Until it exists, a typographic DFX Academy lockup is rendered instead.
